# Área da Empresa (futuro)

Painel corporativo: corridas do time, centro de custo e fatura única. Entra
como `app/(dashboard)/empresa/`, com tabela `companies` e relação N:N com
`users` (funcionários). Service em `services/companies`.
