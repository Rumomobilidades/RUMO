# Área do Passageiro (futuro)

Solicitação de corrida, histórico de viagens e métodos de pagamento. Entra
como `app/(dashboard)/passageiro/`, com tabela `passengers` (1:1 com `users`)
e service em `services/passengers`. Consome a mesma API REST (`/api/rides`)
que o app mobile usará.
